#ifndef _STATISTICS_H_
#define _STATISTICS_H_

#include "geometry/planar_graph.h"

tuple<double, size_t> angle_difference(geometry::planar_graph_t const& g1, geometry::planar_graph_t const& g2);

#endif
